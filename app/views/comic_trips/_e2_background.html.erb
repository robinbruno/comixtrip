<div id="Element1" class="background-photos-modal background-grid d-none" data-live-preview-target="background2">
  <% @background_elements.each do |element| %>
    <div data-action="click->live-preview#setVig2Background" data-imagename="<%= element.name %>">
      <%= image_tag(element.image_url) %>
    </div>
  <% end %>
</div>
